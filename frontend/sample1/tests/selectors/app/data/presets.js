export const hasConnectedPreset = {
  presets: [{
    id: 'preset 1',
    isConnected: true
  }]
};

export const noConnectedPreset = {
  presets: [{
    id: 'preset 1',
    isConnected: false
  }]
};

